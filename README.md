# Phishing-Email-Detection-Model
A machine learning project built using Scikit-learn to detect phishing emails. The model is trained on labeled datasets of phishing and legitimate emails and uses text and URL-based feature extraction to classify emails as “Phishing” or “Safe.” It also evaluates performance using accuracy metrics and a confusion matrix for model analysis.
